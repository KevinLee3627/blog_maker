<!DOCTYPE html>
<html>
<head>
	<title>FEED</title>
</head>
<body>
	<main>
	<% for(let i = 0; i < posts.length; i++) { %>
		<article>
			<header>
				<h1><%= posts[i].title %></h1>	

				<time pubdate datetime="<%= posts[i].date %>" ><%= posts[i].date %></time>
			</header>
			<% for(let i = 0; i < posts.length; i++) { %>
				<% for(let j = 0; j < posts[i].body.length; j++) { %>
					<p><%= posts[i].body[j] %></p>
				<% } %>
			<% } %>

			<!-- figure out how to break up post body by newlines-->
		</article>
	<% } %>		
	</main>

</body>
</html>